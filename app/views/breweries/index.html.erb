<h1>Breweries</h1>

<ul>
  <% @breweries.each do |b| %>
    <li>
      <%= link_to b.name, b %><br>
    </li>
  <% end %>
</ul>

<%= render partial: 'shared/search', locals: {objects: "breweries", object_name: "brewery_name"} %>
